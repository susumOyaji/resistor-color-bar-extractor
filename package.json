{
  "name": "resistor-color-bar-extractor",
  "version": "1.0.0",
  "description": "Extract color bars from resistor images using Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "get-pixels": "^3.3.3"
  },
  "devDependencies": {
    "wrangler": "^4.53.0"
  }
}
